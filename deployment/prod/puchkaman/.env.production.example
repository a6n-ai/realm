# Puchkaman prod env. The public marketing site needs NOTHING at runtime today
# (no DB/redis/auth). This file is a placeholder for when admin/DB lands.
#
# Nothing to set: the stack has no env_file and no awslogs driver (json-file
# default). If a backend lands and CloudWatch comes with it, AWS_REGION is
# us-east-1 — every region in this repo is.
