# Gradual Foundry → Relay → apps gate. Nested foundry/ and relay/ must stay
# until EXTRACTION.md deletes them; do not delete those trees yet.
name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  foundry:
    name: Foundry git deps
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: node scripts/assert-foundry-from-git.mjs
      # @foundry/* typecheck and test on a6n-ai/foundry CI.

  relay:
    name: Relay git deps
    needs: foundry
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: node scripts/assert-relay-from-git.mjs
      - run: pnpm turbo typecheck --filter=relay
      # @relay/* package tests live on a6n-ai/relay CI. Nested apps/relay stays
      # in this workspace until nested trees are deleted.

  apps:
    name: Realm apps
    needs: [relay, foundry]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo typecheck --filter=tiffin-grab --filter=puchkaman
      # App vitest talks to Postgres. Add a services: postgres job later; do not
      # fail this gate on an empty/no-DB vitest run.
