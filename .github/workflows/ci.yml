# Gradual Foundry → Relay → apps gate. Nested foundry/ and relay/ must stay
# until this workflow is green; do not delete those trees or switch to git deps.
name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  foundry:
    name: Foundry packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo typecheck --filter="@foundry/*"
      - run: pnpm turbo test --filter="@foundry/*"

  foundry-ai-git:
    name: Foundry @foundry/ai from git
    needs: foundry
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: node scripts/assert-foundry-ai-from-git.mjs

  relay:
    name: Relay
    needs: foundry
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo typecheck --filter="@relay/*" --filter=relay
      - run: pnpm turbo test --filter="@relay/*" --filter=relay

  apps:
    name: Realm apps
    needs: [relay, foundry-ai-git]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo typecheck --filter=tiffin-grab --filter=puchkaman
      # App vitest talks to Postgres. Add a services: postgres job later; do not
      # fail this gate on an empty/no-DB vitest run.
